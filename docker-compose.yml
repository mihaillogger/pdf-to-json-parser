version: '3.8'

services:
  parser:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./results:/app/results
      - ./weights:/app/weights
    extra_hosts:
      - "host.docker.internal:host-gateway"